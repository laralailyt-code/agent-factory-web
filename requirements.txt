langgraph>=1.1.0
anthropic>=0.100.0
python-dotenv>=1.0.0
jinja2>=3.1.0
pydantic>=2.0.0
pytest>=8.0.0
httpx>=0.27.0

# === D3.5 · Web SSE dashboard ===
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# === D3.1 · 真打包桌面 .exe(excel_diff demo)需要這三個 ===
# 不需要桌面 demo 的話可以省。RUN_PYINSTALLER=true 時 Deployer 才會用到
pyinstaller>=6.0.0
pandas>=2.0.0
openpyxl>=3.1.0
